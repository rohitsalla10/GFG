Problem Statement :-

Given the string "A string." Print on one line the letter on the index 0, the pointer position and the letter t. 
Update the pointer to pointer+2. Then, in another line print the pointer and the letters r and g of the string (using the pointer).


#include <iostream>
using namespace std;

int main() {
   char str[] = "A string.";
   char *po = str;

   cout << str[0] <<' '<< *po <<' '<<po[3]<<"\n";
   po += 2;
   cout <<*po<<' '<< po[2] <<' '<< po[5];

   return 0;
}
